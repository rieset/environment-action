name: 'environment'
description: 'Use environment and pass secrets as env'
inputs:
  environment:
    description: 'Environment name'
    required: true
outputs:
  report:
    description: 'The time we greeted you'
runs:
  using: 'node12'
  main: 'index.js'
